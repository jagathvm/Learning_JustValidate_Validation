<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />
    <title>Learning - JustValidate</title>
    <!-- Favicon for the page -->
    <link
      data-rh="true"
      rel="icon"
      href="https://just-validate.dev/img/favicon.ico"
    />
    <!-- Bootstrap CSS for styling -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <!-- Custom stylesheet -->
    <link
      rel="stylesheet"
      href="/stylesheets/style.css"
    />
  </head>

  <body>
    <!-- Main section to center the form -->
    <section
      class="d-flex justify-content-center align-items-center vh-100"
    >
      <!-- Form with Bootstrap styling -->
      <form
        id="form"
        class="p-4 border border-2 rounded-4"
      >
        <!-- Logo of JustValidate -->
        <div class="mb-4 p-2">
          <img
            src="https://just-validate.dev/img/logo.svg"
            alt="JustValidate"
            class="themedImage_ToTc themedImage--light_HNdA"
            height="60"
          />
        </div>
        <!-- Email input field with label -->
        <div class="form-floating mb-3 p-1">
          <input
            type="email"
            class="form-control"
            id="floatingEmail"
          />
          <label
            for="floatingEmail"
            id="labelEmail"
            >Email address</label
          >
        </div>
        <!-- Password input field with label -->
        <div class="form-floating mb-4 p-1">
          <input
            type="password"
            class="form-control"
            id="floatingPassword"
          />
          <label
            for="floatingPassword"
            id="labelPassword"
            >Password</label
          >
        </div>
        <!-- Checkbox input -->
        <div class="form-check mb-4 ms-2">
          <input
            type="checkbox"
            class="form-check-input"
            id="checkbox"
          />
          <label
            class="form-check-label"
            for="checkbox"
            >Check me out</label
          >
        </div>
        <!-- Submit button -->
        <div class="d-grid p-2">
          <button
            type="submit"
            class="btn btn-primary"
          >
            Submit
          </button>
        </div>
      </form>
    </section>

    <!-- Bootstrap JS for interactivity -->
    <script
      defer
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
    <!-- JustValidate library for form validation -->
    <script
      defer
      src="https://unpkg.com/just-validate@latest/dist/just-validate.production.min.js"
    ></script>
    <!-- Custom JavaScript file -->
    <script
      defer
      src="/scripts/justValidate.js"
    ></script>
  </body>
</html>
